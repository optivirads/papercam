services:
  - type: web
    name: pscmaster-api
    env: node
    region: singapore
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
